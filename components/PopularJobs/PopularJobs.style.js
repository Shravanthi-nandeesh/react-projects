import { StyleSheet } from "react-native";
import { SPACING } from "../../constants";

const PopularJobsStyle = StyleSheet.create({
    popularJobsSection: {
        width: "100%",
        marginTop:SPACING.large
    },
    headerWrapper: {
        flexDirection: "row",
        flex: 1,
        justifyContent: "space-between",
        alignItems: "center"
    },
    headerButton: {},
    cardSection:{
        
    }
})
export default PopularJobsStyle;