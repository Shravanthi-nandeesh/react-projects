import { StyleSheet } from "react-native";
import { COLORS, FONT, SIZES, SPACING } from "../../constants";

const nearByStyle = StyleSheet.create({
    headerWrapper: {
        flexDirection: "row",
        justifyContent: "space-between",
        marginBottom: SPACING.medium
    },
    header: {},
    headerButton: {},
   
  

})
export default nearByStyle;